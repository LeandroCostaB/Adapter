/**
 * Moedas suportadas pela integração com o gateway externo.
 * Usar um enum (em vez de String livre) evita erros de digitação e
 * concentra num único lugar quais moedas o sistema aceita.
 */
public enum Currency {
    BRL,
    USD,
    EUR
}
